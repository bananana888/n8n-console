# 更新日志

## [4.2.1] - 2026-08-24

- **修复启动后网页报 "Cannot GET /"**：全局 n8n 的前端 UI 包（`n8n-editor-ui`）物理缺失，导致前端资源无法加载（后端 `/healthz` 正常、n8n 进程也正常）；补装对应版本（2.35.4）即可恢复
- **启动流程等前端就绪**：健康检查在后端就绪后继续等待前端资源生成完成（n8n 每次启动会重建前端缓存 `~\.cache\n8n`，约 30-60 秒，期间访问 `/` 会返回 404），前端就绪后才提示成功/打开浏览器，不再撞上 404 窗口

## [4.2.0] - 2026-08-24

- **安装检测「先系统后便携」**：node 步骤先看系统/PATH 是否有可用 node（版本满足即跳过下载），系统缺失才下载便携 node 到当前文件夹；n8n 依旧按入口探测（便携/本地/全局任一即视为已装）。系统有 node 但没 n8n 时，n8n 直接装到当前文件夹 `tools\` 下（不再写进系统 node 目录污染系统环境）
- **卸载可自选删除程度**：`uninstall.ps1` 支持交互菜单（1 控制台程序 / 2 运行时缓存 / 3 便携 node+n8n / 4 全局 n8n / 5 数据）与参数模式（`-Console` / `-Cache` / `-Portable` / `-GlobalN8n` / `-Data`），可精确控制删除范围；原 `-RemoveData` / `-RemoveGlobalN8n` 参数继续兼容

## [4.1.4] - 2026-08-24

- **修复静默启动告警失效**：环境缺失时 `-Silent start` 的提示串内嵌未转义双引号，被解析为多余参数导致运行时报错；改用中文引号『』
- **安装包版本与应用同步**：WiX 安装包版本改由 `build.ps1` 从主脚本 `AppVersion` 动态注入，不再硬编码 4.0.0（MajorUpgrade 版本判断恢复正常）
- **启动器路径动态化**：`launcher.vbs` 按脚本自身目录定位 `n8n-control.ps1`，不再硬编码 `D:\APP\...` 过时路径
- **日志写入兜底**：`Write-CtrlLog`/`Write-FatalLog` 加 try/catch，日志文件被锁或权限不足时不再中断主流程
- **单实例锁加固**：Mutex 名改用 `Local\` 会话局部命名空间 + 脚本路径哈希，防止跨会话误锁与恶意预占同名互斥体
- **修复 README 下载链接仓库名**：`n8n-` → `n8n-console`

## [4.1.3] - 2026-08-24

- **修复启动后不自动打开 Web UI**：点「启动」时若 n8n 已在运行（上次残留实例 / 状态接管场景），也会自动打开浏览器访问编辑器；此前只在"本次新启动成功"时才打开。覆盖 GUI 已运行分支、后台接管分支、CLI `start`（`-Silent` 除外）

## [4.1.2] - 2026-08-24

- **修复端口占用误报**：启动时端口被占不再一律报"被其他程序占用"——先通过 `Get-NetTCPConnection` 识别占用进程，若是 n8n 自身（上次控制台退出后残留的实例，或状态文件脱节），自动把真实 PID 接管回状态文件并提示先停止，可从控制台正常停止管理
- **修复状态脱节**：健康检查超时但进程仍存活时保留 PID 文件（原先误清空，导致下次启动把"自己的 n8n"误判为"其他程序占端口"）

## [4.1.1] - 2026-08-24

- **修复便携 node 检测**：`node-portable` 只判定便携目录（`tools\node-<版本>`）是否就绪；系统即便有全局 node 也会按配置下载便携 node，保证"换机器拷贝即用"的自包含特性
- **修复下载提示与实际不符**：n8n 依赖下载阶段实时显示已用时间（"已用时 3 分 12 秒，通常需 5-10 分钟"），不再固定"可能需要几分钟"
- **修复进度条疑似不动**：安装进度轮询只在模式变化时切换 `ProgressBar.Style`，避免每 500ms 反复 Set 重启 Marquee 动画导致视觉静止
- **修复安装时呼吸灯不明显**：检测/安装/启动中状态圆点由固定金色改为金色呼吸动画（金↔深金），与运行中的绿色呼吸呼应
- **修复安装包启动卡顿**：点 ▶ 启动的环境自检移到后台 runspace（`npm root -g`/`node --version` 不再阻塞 UI 线程），界面保持流畅；新增"检测中"状态
- **版本号同步**：`n8n.ps1` 的 `AppVersion` 与 release 对齐（4.1.1）

## [4.1.0] - 2026-08-23

- **家用机适配**：`Console.Home` 留空自动用脚本所在目录（换目录/换机器拷贝即用）；n8n 改用 npm 全局安装入口（`D:\npm-global`）；删除 `N8N_USER_FOLDER` 双嵌套（家用机数据走默认 `~\.n8n`）
- **修复家用机 n8n 启动卡死**：系统代理（`HTTP_PROXY` + `NODE_USE_ENV_PROXY=1`）导致 node 22.22 的 undici 外联挂起，进程存活但不监听端口；配置注入 `NODE_USE_ENV_PROXY=0` 直连外网（实测 12s 就绪）
- **服务入口自动探测**：便携 node / 配置绝对路径 / 本地安装 / npm 全局 / PATH shim 五级定位真实入口；npm 不在 PATH 时回退便携 node 的 `npm.cmd`；npm-install 完成判定改为「入口可探测到」
- **源码兼容性**：exe 定位逐级回退（便携 node → 配置绝对路径 → PATH）；入口失效自动探测替换；`create_shortcut.py` 硬编码路径改为脚本目录动态推导
- **打包完善**：WiX 新增 `license.rtf` 许可协议，Bundle/installer 同步调整
- **项目减重**：删除嵌套仓库副本（136MB）、Inno 遗留 `installer.iss`、WiX 中间产物与 worktree 残留
- 源码文件统一 CRLF 行尾

## [4.0.0] - 2026-08-21

- **多实例壳子**：`-ConfigFile` 指定配置，一套壳子复用管任意命令行服务
- **环境自检 + 自动安装**：缺 node/n8n 自动下载安装（便携 node + npm install + 数据初始化），窗口内进度条实时展示
- **日志轮转**：超过 2MB 自动滚动，保留 3 份
- **单实例锁**：防重复打开控制台
- `-Version` 查看版本、配置缺失校验、错误上下文
- GUI：异步启动（不冻结）、绿灯慢闪、内联 toast、关窗直接退出

## [3.0.0] - 2026-08-21

- 单文件 660 行重构拆分为 `n8n.ps1` + `lib/` + `n8n.config.psd1`（配置外置）
- 修复：node 版本导致启动即退、EPERM 缓存锁、PID 残留自愈、PS5.1 事件闭包陷阱、永久黄灯
- 启动异步化、绿灯慢闪、无托盘直退、进程死亡快速检测、exe 启动预检
